# ZoneRedundancyEnum

Partitions must be replicated in as many zones as possible:
as many zones as there are replicas, if there are enough distinct
zones, or at least one in each zone otherwise.


## Values

| Name                        | Value                       |
| --------------------------- | --------------------------- |
| `ZoneRedundancyEnumMaximum` | maximum                     |